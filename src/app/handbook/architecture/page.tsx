import {FlowFigure} from "@/components/learning-ui";
export default function Architecture(){return <main className="shell narrow-shell"><section className="phase-head"><div className="eyebrow">Handbook · architecture</div><h1>The map that should remain in your head throughout the course.</h1><p>Frameworks change details. Responsibility, trust, ownership, and evidence are the durable mental models.</p></section><section className="section"><h2>Core request path</h2><FlowFigure title="Figure H.1 — Create/update request" steps={["User + browser","Next.js UI/form","HTTP API client","NestJS auth/validation","Service/policy","Prisma","PostgreSQL"]} caption="The response walks back through the same boundaries. Ask what each layer owns and what errors it may produce."/></section><section className="section"><h2>State ownership</h2><div className="reference-table"><div className="reference-row"><code>URL state</code><p>Shareable/navigation state: filters, page, sort, selected view when appropriate.</p></div><div className="reference-row"><code>Server state</code><p>Authoritative remote data: TanStack Query / server fetching, not duplicated into local state.</p></div><div className="reference-row"><code>Form state</code><p>Temporary user editing intent: React Hook Form + validation feedback.</p></div><div className="reference-row"><code>Local UI state</code><p>Ephemeral interaction: menu open, dialog tab, hover/preview.</p></div><div className="reference-row"><code>Global client state</code><p>Use only for genuine cross-cutting client-owned values that do not fit the categories above.</p></div></div></section><section className="section"><h2>Production topology</h2><FlowFigure title="Figure H.2 — Production release path" steps={["Git commit SHA","CI verification","Immutable artifact","Vercel web + API host","Supabase services","DNS/TLS/CORS","Monitoring"]} caption="Migrations, secrets, smoke tests, health checks, and rollback are part of the release system, not afterthoughts."/></section><section className="section"><div className="callout"><b>Architecture test:</b> for any feature, explain where input is validated, identity is verified, permission is checked, business rules run, durable data changes, errors are mapped, and evidence is logged. If two layers both claim the same source of truth, investigate.</div></section></main>}
