import Link from "next/link"; import {notFound} from "next/navigation"; import {getDeepDiveMeta,getDeepDiveTrack} from "@/lib/course";
export function generateStaticParams(){return getDeepDiveMeta().tracks.map(t=>({track:t.slug}))}
export default async function TrackPage({params}:{params:Promise<{track:string}>}){const {track}=await params;const t=getDeepDiveTrack(track);if(!t)notFound();return <main className="shell"><section className="phase-head"><div className="eyebrow">Deep Instruction · {t.audience}</div><h1>{t.title}</h1><p>{t.description}</p></section><section className="section"><div className="lesson-list">{t.lessons.map((l,i)=><Link className="lesson-row" href={`/deep-dives/${t.slug}/${l.slug}`} key={l.slug}><span className="lesson-num">{String(i+1).padStart(2,"0")}</span><div><h3>{l.title}</h3><p>{l.why}</p></div><small>code-first</small></Link>)}</div></section></main>}
