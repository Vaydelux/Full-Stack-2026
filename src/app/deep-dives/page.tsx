import Link from "next/link";import {getDeepDiveMeta} from "@/lib/course";
export default function DeepDives(){const m=getDeepDiveMeta();const lessons=m.tracks.reduce((a,t)=>a+t.lessonCount,0);return <main className="shell"><section className="hero"><div><div className="eyebrow">Optional slower teaching</div><h1>{m.title}</h1><p>{m.subtitle}</p><div className="actions"><Link className="button" href={`/deep-dives/${m.tracks[0].slug}`}>Start first deep dive →</Link><Link className="button ghost" href="/instructor">Back to Guided Course</Link></div></div><aside className="hero-card"><div className="eyebrow">Use these when needed</div><div className="metrics"><div className="metric"><b>{m.tracks.length}</b><span>tracks</span></div><div className="metric"><b>{lessons}</b><span>deep lessons</span></div><div className="metric"><b>1</b><span>transfer challenge / lesson</span></div></div><p className="muted">The main course is the recommended path. Open a deep dive when a concept needs another angle, a slower example, or extra practice.</p></aside></section><section className="section"><div className="eyebrow">Deep-dive library</div><h2>Focused supplemental tracks</h2><div className="grid cards">{m.tracks.map(t=><Link className="card big-card" key={t.slug} href={`/deep-dives/${t.slug}`}><div className="eyebrow">{t.audience} · {t.lessonCount} lessons</div><h3>{t.title}</h3><p className="muted">{t.description}</p></Link>)}</div></section></main>}
