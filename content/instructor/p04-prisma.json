{
  "number": 10,
  "slug": "p04-prisma",
  "title": "Prisma 7 — ORM, Migrations & Data Modeling",
  "audience": "Phases 09–11",
  "description": "Use Prisma without losing sight of the SQL schema, migration risk, and database behavior underneath.",
  "lessonCount": 15,
  "lessons": [
    {
      "number": 1,
      "slug": "schema",
      "title": "Prisma Schema Anatomy",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Prisma Schema Anatomy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that generator, datasource, model fields, defaults, native type hints, and table/column mappings are all explicit in one schema. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Prisma Schema Anatomy in terms of inputs, owner, outputs, and failure modes.",
        "Implement Prisma Schema Anatomy incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Prisma Schema Anatomy failure from evidence.",
        "Apply Prisma Schema Anatomy to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Prisma Schema Anatomy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that generator, datasource, model fields, defaults, native type hints, and table/column mappings are all explicit in one schema. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Prisma Schema Anatomy in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Prisma Schema Anatomy in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Prisma Schema Anatomy. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Prisma Schema Anatomy.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "prisma",
        "code": "generator client { provider = \"prisma-client\" output = \"../src/generated/prisma\" }\ndatasource db { provider = \"postgresql\" }\nmodel Task {\n  id        String   @id @default(uuid()) @db.Uuid\n  title     String\n  done      Boolean  @default(false)\n  createdAt DateTime @default(now()) @map(\"created_at\")\n  @@map(\"tasks\")\n}",
        "expected": "Generator, datasource, model fields, defaults, native type hints, and table/column mappings are all explicit in one schema."
      },
      "expectedEvidence": [
        "Generator, datasource, model fields, defaults, native type hints, and table/column mappings are all explicit in one schema.",
        "You can explain Prisma Schema Anatomy in plain language without reading the page.",
        "You can identify where Prisma Schema Anatomy belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Prisma Schema Anatomy**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Prisma Schema Anatomy example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Prisma Schema Anatomy."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Prisma Schema Anatomy failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Prisma Schema Anatomy without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Prisma Schema Anatomy is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Prisma Schema Anatomy” mean in this course?",
          "answer": "This lesson is about Prisma Schema Anatomy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that generator, datasource, model fields, defaults, native type hints, and table/column mappings are all explicit in one schema. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Prisma Schema Anatomy explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Prisma Schema Anatomy example produce?",
          "answer": "Generator, datasource, model fields, defaults, native type hints, and table/column mappings are all explicit in one schema.",
          "explanation": "That evidence is the observable contract for the Prisma Schema Anatomy example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Prisma Schema Anatomy connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Prisma Schema Anatomy to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Prisma Schema Anatomy failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Prisma Schema Anatomy, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Prisma Schema Anatomy instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Prisma Schema Anatomy, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Prisma Schema Anatomy understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Prisma Schema Anatomy must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Prisma Schema Anatomy without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 2033,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Prisma Schema Anatomy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that generator, datasource, model fields, defaults, native type hints, and table/column mappings are all explicit in one schema. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Prisma Schema Anatomy.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Prisma Schema Anatomy and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Prisma Schema Anatomy is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Prisma Schema Anatomy belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Prisma Schema Anatomy simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 2,
      "slug": "client",
      "title": "Generated Prisma Client",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Generated Prisma Client. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the generated client exposes typed model operations based on the current Prisma schema. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Generated Prisma Client in terms of inputs, owner, outputs, and failure modes.",
        "Implement Generated Prisma Client incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Generated Prisma Client failure from evidence.",
        "Apply Generated Prisma Client to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Generated Prisma Client. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the generated client exposes typed model operations based on the current Prisma schema. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Generated Prisma Client in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Generated Prisma Client in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Generated Prisma Client. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Generated Prisma Client.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "typescript",
        "code": "import { PrismaClient } from \"./generated/prisma/client\";\nconst prisma = new PrismaClient();\nconst tasks = await prisma.task.findMany({ where: { done: false } });",
        "expected": "The generated client exposes typed model operations based on the current Prisma schema."
      },
      "expectedEvidence": [
        "The generated client exposes typed model operations based on the current Prisma schema.",
        "You can explain Generated Prisma Client in plain language without reading the page.",
        "You can identify where Generated Prisma Client belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Generated Prisma Client**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Generated Prisma Client example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Generated Prisma Client."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Generated Prisma Client failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Generated Prisma Client without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Generated Prisma Client is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Generated Prisma Client” mean in this course?",
          "answer": "This lesson is about Generated Prisma Client. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the generated client exposes typed model operations based on the current Prisma schema. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Generated Prisma Client explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Generated Prisma Client example produce?",
          "answer": "The generated client exposes typed model operations based on the current Prisma schema.",
          "explanation": "That evidence is the observable contract for the Generated Prisma Client example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Generated Prisma Client connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Generated Prisma Client to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Generated Prisma Client failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Generated Prisma Client, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Generated Prisma Client instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Generated Prisma Client, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Generated Prisma Client understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Generated Prisma Client must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Generated Prisma Client without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1976,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Generated Prisma Client. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the generated client exposes typed model operations based on the current Prisma schema. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Generated Prisma Client.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Generated Prisma Client and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Generated Prisma Client is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Generated Prisma Client belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Generated Prisma Client simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 3,
      "slug": "service",
      "title": "PrismaService in NestJS",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about PrismaService in NestJS. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that nest can inject one Prisma client wrapper and close its connections cleanly during shutdown. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain PrismaService in NestJS in terms of inputs, owner, outputs, and failure modes.",
        "Implement PrismaService in NestJS incrementally and predict observable evidence before execution.",
        "Diagnose a controlled PrismaService in NestJS failure from evidence.",
        "Apply PrismaService in NestJS to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about PrismaService in NestJS. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that nest can inject one Prisma client wrapper and close its connections cleanly during shutdown. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain PrismaService in NestJS in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for PrismaService in NestJS in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to PrismaService in NestJS. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by PrismaService in NestJS.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "typescript",
        "code": "@Injectable()\nexport class PrismaService extends PrismaClient implements OnModuleDestroy {\n  async onModuleDestroy() { await this.$disconnect(); }\n}",
        "expected": "Nest can inject one Prisma client wrapper and close its connections cleanly during shutdown."
      },
      "expectedEvidence": [
        "Nest can inject one Prisma client wrapper and close its connections cleanly during shutdown.",
        "You can explain PrismaService in NestJS in plain language without reading the page.",
        "You can identify where PrismaService in NestJS belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **PrismaService in NestJS**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the PrismaService in NestJS example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by PrismaService in NestJS."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional PrismaService in NestJS failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of PrismaService in NestJS without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for PrismaService in NestJS is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “PrismaService in NestJS” mean in this course?",
          "answer": "This lesson is about PrismaService in NestJS. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that nest can inject one Prisma client wrapper and close its connections cleanly during shutdown. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful PrismaService in NestJS explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked PrismaService in NestJS example produce?",
          "answer": "Nest can inject one Prisma client wrapper and close its connections cleanly during shutdown.",
          "explanation": "That evidence is the observable contract for the PrismaService in NestJS example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does PrismaService in NestJS connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects PrismaService in NestJS to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional PrismaService in NestJS failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For PrismaService in NestJS, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered PrismaService in NestJS instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind PrismaService in NestJS, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of PrismaService in NestJS understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, PrismaService in NestJS must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain PrismaService in NestJS without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1979,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about PrismaService in NestJS. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that nest can inject one Prisma client wrapper and close its connections cleanly during shutdown. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for PrismaService in NestJS.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of PrismaService in NestJS and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so PrismaService in NestJS is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why PrismaService in NestJS belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe PrismaService in NestJS simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 4,
      "slug": "first-model",
      "title": "First Task Model",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about First Task Model. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the ORM model expresses the same identity, workspace ownership, status, and naming decisions as the relational design. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain First Task Model in terms of inputs, owner, outputs, and failure modes.",
        "Implement First Task Model incrementally and predict observable evidence before execution.",
        "Diagnose a controlled First Task Model failure from evidence.",
        "Apply First Task Model to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about First Task Model. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the ORM model expresses the same identity, workspace ownership, status, and naming decisions as the relational design. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain First Task Model in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for First Task Model in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to First Task Model. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by First Task Model.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "prisma",
        "code": "model Task {\n  id          String    @id @default(uuid()) @db.Uuid\n  workspaceId String    @db.Uuid @map(\"workspace_id\")\n  title       String\n  status      TaskStatus @default(OPEN)\n  createdAt   DateTime  @default(now()) @map(\"created_at\")\n  @@map(\"tasks\")\n}\nenum TaskStatus { OPEN DONE }",
        "expected": "The ORM model expresses the same identity, workspace ownership, status, and naming decisions as the relational design."
      },
      "expectedEvidence": [
        "The ORM model expresses the same identity, workspace ownership, status, and naming decisions as the relational design.",
        "You can explain First Task Model in plain language without reading the page.",
        "You can identify where First Task Model belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **First Task Model**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the First Task Model example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by First Task Model."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional First Task Model failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of First Task Model without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for First Task Model is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “First Task Model” mean in this course?",
          "answer": "This lesson is about First Task Model. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the ORM model expresses the same identity, workspace ownership, status, and naming decisions as the relational design. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful First Task Model explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked First Task Model example produce?",
          "answer": "The ORM model expresses the same identity, workspace ownership, status, and naming decisions as the relational design.",
          "explanation": "That evidence is the observable contract for the First Task Model example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does First Task Model connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects First Task Model to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional First Task Model failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For First Task Model, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered First Task Model instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind First Task Model, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of First Task Model understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, First Task Model must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain First Task Model without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 2025,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about First Task Model. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the ORM model expresses the same identity, workspace ownership, status, and naming decisions as the relational design. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for First Task Model.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of First Task Model and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so First Task Model is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why First Task Model belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe First Task Model simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 5,
      "slug": "migrate-dev",
      "title": "Development Migrations",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command. In this course, we connect it directly to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit, so you can see where the idea lives in a real project instead of memorizing a definition.",
      "objectives": [
        "Explain Development Migrations in terms of inputs, owner, outputs, and failure modes.",
        "Implement Development Migrations incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Development Migrations failure from evidence.",
        "Apply Development Migrations to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "Start with the plain-English idea. A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Development Migrations in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Development Migrations in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Development Migrations. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Development Migrations.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "powershell",
        "code": "pnpm exec prisma migrate dev --name create_tasks\npnpm exec prisma generate",
        "expected": "Prisma creates a versioned migration for the local development change, applies it, and regenerates the client."
      },
      "expectedEvidence": [
        "Prisma creates a versioned migration for the local development change, applies it, and regenerates the client.",
        "You can explain Development Migrations in plain language without reading the page.",
        "You can identify where Development Migrations belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Development Migrations**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Development Migrations example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Development Migrations."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Development Migrations failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Development Migrations without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Development Migrations is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Development Migrations” mean?",
          "answer": "A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command.",
          "explanation": "A good Development Migrations explanation describes the behavior or boundary, not only the API or syntax used to implement it."
        },
        {
          "question": "What concrete evidence should the worked Development Migrations example produce?",
          "answer": "Prisma creates a versioned migration for the local development change, applies it, and regenerates the client.",
          "explanation": "That evidence is the observable contract for the Development Migrations example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Development Migrations connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Development Migrations to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Development Migrations failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Development Migrations, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Development Migrations instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Development Migrations, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Development Migrations understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Development Migrations must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Development Migrations without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1849,
      "casualGuide": {
        "plainEnglish": [
          "A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Development Migrations.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Development Migrations and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Development Migrations is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Development Migrations belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Development Migrations simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 6,
      "slug": "inspect-sql",
      "title": "Inspect Generated SQL",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Inspect Generated SQL. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that you review the actual database operations Prisma intends to run rather than treating the migration command as magic. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Inspect Generated SQL in terms of inputs, owner, outputs, and failure modes.",
        "Implement Inspect Generated SQL incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Inspect Generated SQL failure from evidence.",
        "Apply Inspect Generated SQL to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Inspect Generated SQL. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that you review the actual database operations Prisma intends to run rather than treating the migration command as magic. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Inspect Generated SQL in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Inspect Generated SQL in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Inspect Generated SQL. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Inspect Generated SQL.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "sql",
        "code": "-- prisma/migrations/..._create_tasks/migration.sql\nCREATE TYPE \"TaskStatus\" AS ENUM ('OPEN', 'DONE');\nCREATE TABLE \"tasks\" (...);\nCREATE INDEX \"tasks_workspace_id_created_at_idx\" ON \"tasks\"(\"workspace_id\", \"created_at\" DESC);",
        "expected": "You review the actual database operations Prisma intends to run rather than treating the migration command as magic."
      },
      "expectedEvidence": [
        "You review the actual database operations Prisma intends to run rather than treating the migration command as magic.",
        "You can explain Inspect Generated SQL in plain language without reading the page.",
        "You can identify where Inspect Generated SQL belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Inspect Generated SQL**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Inspect Generated SQL example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Inspect Generated SQL."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Inspect Generated SQL failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Inspect Generated SQL without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Inspect Generated SQL is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Inspect Generated SQL” mean in this course?",
          "answer": "This lesson is about Inspect Generated SQL. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that you review the actual database operations Prisma intends to run rather than treating the migration command as magic. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Inspect Generated SQL explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Inspect Generated SQL example produce?",
          "answer": "You review the actual database operations Prisma intends to run rather than treating the migration command as magic.",
          "explanation": "That evidence is the observable contract for the Inspect Generated SQL example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Inspect Generated SQL connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Inspect Generated SQL to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Inspect Generated SQL failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Inspect Generated SQL, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Inspect Generated SQL instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Inspect Generated SQL, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Inspect Generated SQL understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Inspect Generated SQL must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Inspect Generated SQL without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 2015,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Inspect Generated SQL. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that you review the actual database operations Prisma intends to run rather than treating the migration command as magic. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Inspect Generated SQL.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Inspect Generated SQL and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Inspect Generated SQL is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Inspect Generated SQL belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Inspect Generated SQL simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 7,
      "slug": "relations",
      "title": "Relations",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Relations. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the relation maps the foreign-key field to the referenced model and expresses delete behavior. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Relations in terms of inputs, owner, outputs, and failure modes.",
        "Implement Relations incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Relations failure from evidence.",
        "Apply Relations to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Relations. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the relation maps the foreign-key field to the referenced model and expresses delete behavior. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Relations in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Relations in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Relations. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Relations.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "prisma",
        "code": "model Workspace { id String @id @default(uuid()) @db.Uuid tasks Task[] }\nmodel Task {\n  id String @id @default(uuid()) @db.Uuid\n  workspaceId String @db.Uuid\n  workspace Workspace @relation(fields: [workspaceId], references: [id], onDelete: Cascade)\n}",
        "expected": "The relation maps the foreign-key field to the referenced model and expresses delete behavior."
      },
      "expectedEvidence": [
        "The relation maps the foreign-key field to the referenced model and expresses delete behavior.",
        "You can explain Relations in plain language without reading the page.",
        "You can identify where Relations belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Relations**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Relations example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Relations."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Relations failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Relations without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Relations is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Relations” mean in this course?",
          "answer": "This lesson is about Relations. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the relation maps the foreign-key field to the referenced model and expresses delete behavior. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Relations explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Relations example produce?",
          "answer": "The relation maps the foreign-key field to the referenced model and expresses delete behavior.",
          "explanation": "That evidence is the observable contract for the Relations example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Relations connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Relations to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Relations failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Relations, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Relations instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Relations, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Relations understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Relations must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Relations without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1923,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Relations. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the relation maps the foreign-key field to the referenced model and expresses delete behavior. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Relations.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Relations and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Relations is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Relations belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Relations simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 8,
      "slug": "indexes",
      "title": "Indexes in Prisma",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work. In this course, we connect it directly to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit, so you can see where the idea lives in a real project instead of memorizing a definition.",
      "objectives": [
        "Explain Indexes in Prisma in terms of inputs, owner, outputs, and failure modes.",
        "Implement Indexes in Prisma incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Indexes in Prisma failure from evidence.",
        "Apply Indexes in Prisma to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "Start with the plain-English idea. An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Indexes in Prisma in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Indexes in Prisma in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Indexes in Prisma. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Indexes in Prisma.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "prisma",
        "code": "model Task {\n  id          String   @id @default(uuid()) @db.Uuid\n  workspaceId String   @db.Uuid\n  createdAt   DateTime @default(now())\n  @@index([workspaceId, createdAt(sort: Desc), id(sort: Desc)])\n}",
        "expected": "The schema records the index shape that supports the main workspace task-list ordering."
      },
      "expectedEvidence": [
        "The schema records the index shape that supports the main workspace task-list ordering.",
        "You can explain Indexes in Prisma in plain language without reading the page.",
        "You can identify where Indexes in Prisma belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Indexes in Prisma**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Indexes in Prisma example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Indexes in Prisma."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Indexes in Prisma failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Indexes in Prisma without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Indexes in Prisma is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Indexes in Prisma” mean?",
          "answer": "An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work.",
          "explanation": "A good Indexes in Prisma explanation describes the behavior or boundary, not only the API or syntax used to implement it."
        },
        {
          "question": "What concrete evidence should the worked Indexes in Prisma example produce?",
          "answer": "The schema records the index shape that supports the main workspace task-list ordering.",
          "explanation": "That evidence is the observable contract for the Indexes in Prisma example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Indexes in Prisma connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Indexes in Prisma to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Indexes in Prisma failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Indexes in Prisma, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Indexes in Prisma instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Indexes in Prisma, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Indexes in Prisma understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Indexes in Prisma must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Indexes in Prisma without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1902,
      "casualGuide": {
        "plainEnglish": [
          "An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Indexes in Prisma.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Indexes in Prisma and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Indexes in Prisma is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Indexes in Prisma belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Indexes in Prisma simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 9,
      "slug": "seed",
      "title": "Seed Data",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Seed Data. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that a repeatable seed creates a small realistic workspace and related tasks for local development/tests. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Seed Data in terms of inputs, owner, outputs, and failure modes.",
        "Implement Seed Data incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Seed Data failure from evidence.",
        "Apply Seed Data to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Seed Data. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that a repeatable seed creates a small realistic workspace and related tasks for local development/tests. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Seed Data in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Seed Data in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Seed Data. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Seed Data.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "typescript",
        "code": "await prisma.workspace.create({\n  data: {\n    name: \"Demo Workspace\",\n    tasks: { create: [{ title: \"First task\" }, { title: \"Second task\" }] },\n  },\n});",
        "expected": "A repeatable seed creates a small realistic workspace and related tasks for local development/tests."
      },
      "expectedEvidence": [
        "A repeatable seed creates a small realistic workspace and related tasks for local development/tests.",
        "You can explain Seed Data in plain language without reading the page.",
        "You can identify where Seed Data belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Seed Data**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Seed Data example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Seed Data."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Seed Data failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Seed Data without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Seed Data is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Seed Data” mean in this course?",
          "answer": "This lesson is about Seed Data. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that a repeatable seed creates a small realistic workspace and related tasks for local development/tests. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Seed Data explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Seed Data example produce?",
          "answer": "A repeatable seed creates a small realistic workspace and related tasks for local development/tests.",
          "explanation": "That evidence is the observable contract for the Seed Data example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Seed Data connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Seed Data to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Seed Data failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Seed Data, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Seed Data instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Seed Data, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Seed Data understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Seed Data must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Seed Data without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1953,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Seed Data. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that a repeatable seed creates a small realistic workspace and related tasks for local development/tests. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Seed Data.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Seed Data and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Seed Data is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Seed Data belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Seed Data simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 10,
      "slug": "transactions",
      "title": "Prisma Transactions",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows. In this course, we connect it directly to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit, so you can see where the idea lives in a real project instead of memorizing a definition.",
      "objectives": [
        "Explain Prisma Transactions in terms of inputs, owner, outputs, and failure modes.",
        "Implement Prisma Transactions incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Prisma Transactions failure from evidence.",
        "Apply Prisma Transactions to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "Start with the plain-English idea. A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Prisma Transactions in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Prisma Transactions in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Prisma Transactions. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Prisma Transactions.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "typescript",
        "code": "await prisma.$transaction(async tx => {\n  await tx.task.update({ where: { id }, data: { status: \"DONE\" } });\n  await tx.auditLog.create({ data: { entityId: id, action: \"TASK_COMPLETED\" } });\n});",
        "expected": "Both ORM writes share one transaction and commit or roll back as one business operation."
      },
      "expectedEvidence": [
        "Both ORM writes share one transaction and commit or roll back as one business operation.",
        "You can explain Prisma Transactions in plain language without reading the page.",
        "You can identify where Prisma Transactions belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Prisma Transactions**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Prisma Transactions example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Prisma Transactions."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Prisma Transactions failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Prisma Transactions without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Prisma Transactions is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Prisma Transactions” mean?",
          "answer": "A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows.",
          "explanation": "A good Prisma Transactions explanation describes the behavior or boundary, not only the API or syntax used to implement it."
        },
        {
          "question": "What concrete evidence should the worked Prisma Transactions example produce?",
          "answer": "Both ORM writes share one transaction and commit or roll back as one business operation.",
          "explanation": "That evidence is the observable contract for the Prisma Transactions example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Prisma Transactions connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Prisma Transactions to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Prisma Transactions failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Prisma Transactions, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Prisma Transactions instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Prisma Transactions, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Prisma Transactions understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Prisma Transactions must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Prisma Transactions without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1857,
      "casualGuide": {
        "plainEnglish": [
          "A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Prisma Transactions.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Prisma Transactions and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Prisma Transactions is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Prisma Transactions belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Prisma Transactions simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 11,
      "slug": "dangerous-change",
      "title": "Dangerous Schema Changes",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: A schema change is dangerous when old application code or existing rows cannot satisfy the new schema immediately. Safe rollouts often expand, backfill, verify, then contract. In this course, we connect it directly to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit, so you can see where the idea lives in a real project instead of memorizing a definition.",
      "objectives": [
        "Explain Dangerous Schema Changes in terms of inputs, owner, outputs, and failure modes.",
        "Implement Dangerous Schema Changes incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Dangerous Schema Changes failure from evidence.",
        "Apply Dangerous Schema Changes to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "Start with the plain-English idea. A schema change is dangerous when old application code or existing rows cannot satisfy the new schema immediately. Safe rollouts often expand, backfill, verify, then contract.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Dangerous Schema Changes in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Dangerous Schema Changes in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Dangerous Schema Changes. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Dangerous Schema Changes.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "prisma",
        "code": "// Risky when old rows exist:\nstatus String\n\n// Safer first expansion:\nstatus String? @default(\"OPEN\")\n// backfill + deploy readers/writers, then make required later",
        "expected": "The staged design preserves compatibility with existing data and rolling application versions."
      },
      "expectedEvidence": [
        "The staged design preserves compatibility with existing data and rolling application versions.",
        "You can explain Dangerous Schema Changes in plain language without reading the page.",
        "You can identify where Dangerous Schema Changes belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Dangerous Schema Changes**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Dangerous Schema Changes example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Dangerous Schema Changes."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Dangerous Schema Changes failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Dangerous Schema Changes without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Dangerous Schema Changes is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Dangerous Schema Changes” mean?",
          "answer": "A schema change is dangerous when old application code or existing rows cannot satisfy the new schema immediately. Safe rollouts often expand, backfill, verify, then contract.",
          "explanation": "A good Dangerous Schema Changes explanation describes the behavior or boundary, not only the API or syntax used to implement it."
        },
        {
          "question": "What concrete evidence should the worked Dangerous Schema Changes example produce?",
          "answer": "The staged design preserves compatibility with existing data and rolling application versions.",
          "explanation": "That evidence is the observable contract for the Dangerous Schema Changes example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Dangerous Schema Changes connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Dangerous Schema Changes to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Dangerous Schema Changes failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Dangerous Schema Changes, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Dangerous Schema Changes instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Dangerous Schema Changes, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Dangerous Schema Changes understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Dangerous Schema Changes must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Dangerous Schema Changes without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1898,
      "casualGuide": {
        "plainEnglish": [
          "A schema change is dangerous when old application code or existing rows cannot satisfy the new schema immediately. Safe rollouts often expand, backfill, verify, then contract.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Dangerous Schema Changes.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Dangerous Schema Changes and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Dangerous Schema Changes is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Dangerous Schema Changes belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Dangerous Schema Changes simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 12,
      "slug": "backfill",
      "title": "Staged Backfills",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Staged Backfills. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the process expands the schema, backfills deterministically, verifies completion, then tightens the constraint. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Staged Backfills in terms of inputs, owner, outputs, and failure modes.",
        "Implement Staged Backfills incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Staged Backfills failure from evidence.",
        "Apply Staged Backfills to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Staged Backfills. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the process expands the schema, backfills deterministically, verifies completion, then tightens the constraint. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Staged Backfills in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Staged Backfills in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Staged Backfills. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Staged Backfills.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "sql",
        "code": "ALTER TABLE tasks ADD COLUMN status text;\nUPDATE tasks SET status = CASE WHEN done THEN 'DONE' ELSE 'OPEN' END WHERE status IS NULL;\nSELECT count(*) FROM tasks WHERE status IS NULL;\nALTER TABLE tasks ALTER COLUMN status SET NOT NULL;",
        "expected": "The process expands the schema, backfills deterministically, verifies completion, then tightens the constraint."
      },
      "expectedEvidence": [
        "The process expands the schema, backfills deterministically, verifies completion, then tightens the constraint.",
        "You can explain Staged Backfills in plain language without reading the page.",
        "You can identify where Staged Backfills belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Staged Backfills**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Staged Backfills example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Staged Backfills."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Staged Backfills failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Staged Backfills without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Staged Backfills is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Staged Backfills” mean in this course?",
          "answer": "This lesson is about Staged Backfills. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the process expands the schema, backfills deterministically, verifies completion, then tightens the constraint. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Staged Backfills explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Staged Backfills example produce?",
          "answer": "The process expands the schema, backfills deterministically, verifies completion, then tightens the constraint.",
          "explanation": "That evidence is the observable contract for the Staged Backfills example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Staged Backfills connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Staged Backfills to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Staged Backfills failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Staged Backfills, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Staged Backfills instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Staged Backfills, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Staged Backfills understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Staged Backfills must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Staged Backfills without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1958,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Staged Backfills. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the process expands the schema, backfills deterministically, verifies completion, then tightens the constraint. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Staged Backfills.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Staged Backfills and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Staged Backfills is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Staged Backfills belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Staged Backfills simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 13,
      "slug": "prod-migrations",
      "title": "Production Migration Strategy",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Production Migration Strategy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that production migration is treated as a release procedure with compatibility and verification, not as an automatic local-development command. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Production Migration Strategy in terms of inputs, owner, outputs, and failure modes.",
        "Implement Production Migration Strategy incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Production Migration Strategy failure from evidence.",
        "Apply Production Migration Strategy to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Production Migration Strategy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that production migration is treated as a release procedure with compatibility and verification, not as an automatic local-development command. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Production Migration Strategy in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Production Migration Strategy in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Production Migration Strategy. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Production Migration Strategy.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "text",
        "code": "1. backup / recovery confidence\n2. review generated SQL\n3. compatibility check with current + next app\n4. apply migration once\n5. verify schema + critical query\n6. deploy compatible application\n7. observe before cleanup migration",
        "expected": "Production migration is treated as a release procedure with compatibility and verification, not as an automatic local-development command."
      },
      "expectedEvidence": [
        "Production migration is treated as a release procedure with compatibility and verification, not as an automatic local-development command.",
        "You can explain Production Migration Strategy in plain language without reading the page.",
        "You can identify where Production Migration Strategy belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Production Migration Strategy**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Production Migration Strategy example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Production Migration Strategy."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Production Migration Strategy failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Production Migration Strategy without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Production Migration Strategy is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Production Migration Strategy” mean in this course?",
          "answer": "This lesson is about Production Migration Strategy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that production migration is treated as a release procedure with compatibility and verification, not as an automatic local-development command. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Production Migration Strategy explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Production Migration Strategy example produce?",
          "answer": "Production migration is treated as a release procedure with compatibility and verification, not as an automatic local-development command.",
          "explanation": "That evidence is the observable contract for the Production Migration Strategy example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Production Migration Strategy connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Production Migration Strategy to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Production Migration Strategy failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Production Migration Strategy, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Production Migration Strategy instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Production Migration Strategy, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Production Migration Strategy understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Production Migration Strategy must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Production Migration Strategy without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 2025,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Production Migration Strategy. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that production migration is treated as a release procedure with compatibility and verification, not as an automatic local-development command. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Production Migration Strategy.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Production Migration Strategy and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Production Migration Strategy is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Production Migration Strategy belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Production Migration Strategy simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 14,
      "slug": "query-shape",
      "title": "Query Shape and N+1",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: N+1 happens when one query loads a list and then additional queries are issued per row. It often looks fine with small data and becomes expensive at scale. In this course, we connect it directly to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit, so you can see where the idea lives in a real project instead of memorizing a definition.",
      "objectives": [
        "Explain Query Shape and N+1 in terms of inputs, owner, outputs, and failure modes.",
        "Implement Query Shape and N+1 incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Query Shape and N+1 failure from evidence.",
        "Apply Query Shape and N+1 to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "Start with the plain-English idea. N+1 happens when one query loads a list and then additional queries are issued per row. It often looks fine with small data and becomes expensive at scale.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Query Shape and N+1 in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Query Shape and N+1 in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Query Shape and N+1. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Query Shape and N+1.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "typescript",
        "code": "const tasks = await prisma.task.findMany({\n  where: { workspaceId },\n  include: { assignee: { select: { id: true, email: true } } },\n});",
        "expected": "Related data is loaded intentionally with one query shape instead of issuing an additional assignee query inside a loop."
      },
      "expectedEvidence": [
        "Related data is loaded intentionally with one query shape instead of issuing an additional assignee query inside a loop.",
        "You can explain Query Shape and N+1 in plain language without reading the page.",
        "You can identify where Query Shape and N+1 belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Query Shape and N+1**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Query Shape and N+1 example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Query Shape and N+1."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Query Shape and N+1 failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Query Shape and N+1 without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Query Shape and N+1 is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Query Shape and N+1” mean?",
          "answer": "N+1 happens when one query loads a list and then additional queries are issued per row. It often looks fine with small data and becomes expensive at scale.",
          "explanation": "A good Query Shape and N+1 explanation describes the behavior or boundary, not only the API or syntax used to implement it."
        },
        {
          "question": "What concrete evidence should the worked Query Shape and N+1 example produce?",
          "answer": "Related data is loaded intentionally with one query shape instead of issuing an additional assignee query inside a loop.",
          "explanation": "That evidence is the observable contract for the Query Shape and N+1 example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Query Shape and N+1 connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Query Shape and N+1 to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Query Shape and N+1 failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Query Shape and N+1, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Query Shape and N+1 instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Query Shape and N+1, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Query Shape and N+1 understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Query Shape and N+1 must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Query Shape and N+1 without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 1994,
      "casualGuide": {
        "plainEnglish": [
          "N+1 happens when one query loads a list and then additional queries are issued per row. It often looks fine with small data and becomes expensive at scale.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Query Shape and N+1.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Query Shape and N+1 and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Query Shape and N+1 is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Query Shape and N+1 belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Query Shape and N+1 simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    },
    {
      "number": 15,
      "slug": "crud",
      "title": "Persistent Task CRUD",
      "audience": "Phases 09–11",
      "why": "Here’s the simple reason this matters: This lesson is about Persistent Task CRUD. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the CRUD operations are now persisted, scoped, and ready to be wrapped by API/business rules rather than living as in-memory arrays. Once you can explain why that happens, the name is much easier to remember. We connect it to the course project so you can see the idea working instead of memorizing a definition.",
      "objectives": [
        "Explain Persistent Task CRUD in terms of inputs, owner, outputs, and failure modes.",
        "Implement Persistent Task CRUD incrementally and predict observable evidence before execution.",
        "Diagnose a controlled Persistent Task CRUD failure from evidence.",
        "Apply Persistent Task CRUD to a new TaskFlow requirement without copying the reference implementation."
      ],
      "prerequisites": [
        "Complete the previous lesson in Prisma 7 — ORM, Migrations & Data Modeling or demonstrate equivalent skill.",
        "Start from a healthy project/checkpoint."
      ],
      "mentalModel": [
        "This lesson is about Persistent Task CRUD. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the CRUD operations are now persisted, scoped, and ready to be wrapped by API/business rules rather than living as in-memory arrays. Once you can explain why that happens, the name is much easier to remember.",
        "Now place it in TaskFlow. This lesson belongs to the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. Ask four questions while you work: what enters this boundary, who owns the decision, what dependency does it use, and what evidence proves the result?",
        "Keep the change small. Predict what you should see before you run anything, then compare the prediction with the real terminal, browser, HTTP, database, test, container, or CI evidence. If they differ, debug the first mismatch instead of editing several places at random.",
        "The goal is not to remember the exact snippet below. The goal is to be able to explain Persistent Task CRUD in your own words, rebuild the core behavior, and adapt it when the requirement changes."
      ],
      "currentState": "You are working inside the Prisma 7 — ORM, Migrations & Data Modeling track. The project is healthy before this lesson, and the learner has completed the preceding checkpoint.",
      "beforeYouCode": [
        "Open the project root in VS Code and a terminal.",
        "Confirm you are on the intended Git branch and have no unrelated uncommitted changes.",
        "Run the previous checkpoint verification before changing code.",
        "Keep browser DevTools, API client, database console, or Docker logs visible when they are relevant to the boundary."
      ],
      "steps": [
        "State the requirement for Persistent Task CRUD in one observable sentence. Avoid implementation words at first; describe what a user, caller, compiler, database, or deployment should observe.",
        "Confirm the baseline is healthy. Run the narrowest existing verification and record the result so a new failure can be attributed to this lesson rather than an earlier problem.",
        "Inspect the current boundary relevant to Persistent Task CRUD. Identify the input, owner, dependency, output, and failure mode before adding code.",
        "Read the working example below without running it. Predict the exact output, HTTP status, compiler result, database state, UI state, or deployment evidence.",
        "Type the smallest working slice yourself. Do not paste a finished subsystem. Keep the diff small enough that every new line can be explained.",
        "Run the narrowest command or interaction that proves the slice. Compare the observed evidence with the prediction and write down any mismatch.",
        "Extend the slice one step toward TaskFlow. Preserve the same ownership rule while replacing toy data with the real project boundary used by Persistent Task CRUD.",
        "Introduce the controlled failure described below. Capture the first useful error line, request/response, query plan, browser state, test failure, container log, or CI job evidence before editing anything.",
        "Locate the first boundary where expected and actual evidence diverge. Change only the root cause, then rerun exactly the same verification.",
        "Add a repeatable regression check when practical: a test, type assertion, SQL constraint, health check, CI gate, security test, or documented smoke-test step.",
        "Close the lesson and rebuild the core behavior from memory. If you cannot explain one step, reopen only that section rather than copying the whole solution."
      ],
      "example": {
        "language": "typescript",
        "code": "create: prisma.task.create({ data: { workspaceId, title } })\nlist: prisma.task.findMany({ where: { workspaceId }, orderBy: [{ createdAt: \"desc\" }, { id: \"desc\" }] })\nread: prisma.task.findFirst({ where: { id, workspaceId } })\nupdate: prisma.task.update({ where: { id }, data: patch })\ndelete: prisma.task.delete({ where: { id } })",
        "expected": "The CRUD operations are now persisted, scoped, and ready to be wrapped by API/business rules rather than living as in-memory arrays."
      },
      "expectedEvidence": [
        "The CRUD operations are now persisted, scoped, and ready to be wrapped by API/business rules rather than living as in-memory arrays.",
        "You can explain Persistent Task CRUD in plain language without reading the page.",
        "You can identify where Persistent Task CRUD belongs inside the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit and why it does not belong everywhere.",
        "You can change one assumption, reproduce a failure, and use evidence to locate the first broken boundary.",
        "You can rebuild the small example from memory and make one reasonable variation without copying the finished code."
      ],
      "breakIt": "Break the exact assumption this lesson depends on. For **Persistent Task CRUD**, change one input, configuration, ordering rule, permission, state transition, or dependency so the example can no longer satisfy its contract. Before running it, write down the failure you expect. Then run the same verification, capture the first useful symptom, and repair only the root cause.",
      "troubleshooting": [
        {
          "symptom": "Nothing changes or the wrong code path runs.",
          "investigation": "Confirm the exact file/route/process is active, then inspect the closest boundary input before changing implementation."
        },
        {
          "symptom": "The output differs from the prediction.",
          "investigation": "Compare expected and actual evidence in order: input → boundary → dependency → output. Stop at the first mismatch."
        },
        {
          "symptom": "A downstream error hides the real cause.",
          "investigation": "Move one boundary earlier. Check compiler diagnostics, request payload, framework registration, SQL state, container logs, or CI step output before the final symptom."
        },
        {
          "symptom": "The fix works only once.",
          "investigation": "Add a deterministic verification and reset state between runs. Look for hidden cache, environment, transaction, time, ordering, or shared-state dependencies."
        }
      ],
      "exercises": [
        {
          "level": "Follow",
          "task": "Reproduce the Persistent Task CRUD example exactly and explain each line before moving on."
        },
        {
          "level": "Modify",
          "task": "Change one TaskFlow requirement while preserving the ownership rule demonstrated by Persistent Task CRUD."
        },
        {
          "level": "Debug",
          "task": "Reproduce the intentional Persistent Task CRUD failure, capture evidence, and repair only the root cause."
        },
        {
          "level": "Build",
          "task": "Implement a new TaskFlow variation of Persistent Task CRUD without reopening the working example until your first attempt fails."
        },
        {
          "level": "Defend",
          "task": "Explain why the chosen boundary for Persistent Task CRUD is preferable to placing the same rule in an adjacent layer."
        }
      ],
      "knowledgeCheck": [
        {
          "question": "In plain language, what does “Persistent Task CRUD” mean in this course?",
          "answer": "This lesson is about Persistent Task CRUD. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the CRUD operations are now persisted, scoped, and ready to be wrapped by API/business rules rather than living as in-memory arrays. Once you can explain why that happens, the name is much easier to remember.",
          "explanation": "A useful Persistent Task CRUD explanation should describe observable behavior and why it matters, not just repeat the term."
        },
        {
          "question": "What concrete evidence should the worked Persistent Task CRUD example produce?",
          "answer": "The CRUD operations are now persisted, scoped, and ready to be wrapped by API/business rules rather than living as in-memory arrays.",
          "explanation": "That evidence is the observable contract for the Persistent Task CRUD example. Compare it with the real result before changing anything."
        },
        {
          "question": "Where does Persistent Task CRUD connect to the TaskFlow learning project?",
          "answer": "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior.",
          "explanation": "The course connects Persistent Task CRUD to a real project boundary so ownership and failure modes are easier to remember."
        },
        {
          "question": "If the intentional Persistent Task CRUD failure appears, what should you do before editing code?",
          "answer": "Capture the first useful symptom, compare actual evidence with the prediction, and identify the earliest boundary where they diverge.",
          "explanation": "For Persistent Task CRUD, evidence-first debugging prevents random edits and keeps the investigation tied to the actual failure path."
        },
        {
          "question": "What would prove that you mastered Persistent Task CRUD instead of memorizing its example?",
          "answer": "You can rebuild the core idea behind Persistent Task CRUD, change one requirement, diagnose a broken version, and explain why the responsibility lives where you placed it.",
          "explanation": "Transfer and diagnosis are stronger evidence of Persistent Task CRUD understanding than copy-paste success."
        }
      ],
      "productionNotes": [
        "In production, Persistent Task CRUD must have an observable failure mode. Decide what should be logged, measured, tested, constrained, or surfaced without leaking sensitive data.",
        "Prefer explicit contracts over conventions that only one developer understands. The next engineer should be able to locate the owner of this behavior from the project structure and tests.",
        "Do not add abstraction merely because the course mentions it. Introduce additional layers only when they isolate a real volatility, boundary, policy, or testability problem."
      ],
      "exitTicket": [
        "I can explain Persistent Task CRUD without reading the lesson.",
        "I can reproduce the core implementation from memory.",
        "I can show evidence that the implementation works.",
        "I can intentionally break and diagnose the boundary.",
        "I can apply the same idea to a different feature and defend the design decision."
      ],
      "approxWordCount": 2059,
      "casualGuide": {
        "plainEnglish": [
          "This lesson is about Persistent Task CRUD. Keep the idea practical: in the Prisma/database mapping and migration workflow, the worked example should show that the CRUD operations are now persisted, scoped, and ready to be wrapped by API/business rules rather than living as in-memory arrays. Once you can explain why that happens, the name is much easier to remember.",
          "You will use this idea while working on the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. We keep the example small first, then connect it back to TaskFlow so the framework does not hide the underlying behavior."
        ],
        "keyIdeas": [
          "Name the owner: decide which layer or runtime is responsible for Persistent Task CRUD.",
          "Name the input and output: write down what crosses the boundary and what callers can observe.",
          "Predict before running: expected output is part of the exercise, not an afterthought.",
          "Prefer one small change at a time so a failure has a narrow list of possible causes.",
          "Finish by changing the requirement slightly; if the idea still makes sense, you are learning the concept rather than the snippet."
        ],
        "walkthrough": [
          "Read the plain-English explanation of Persistent Task CRUD and say it back in your own words.",
          "Inspect the current project state and identify the smallest file or boundary that must change.",
          "Read the worked example but do not run it yet. Predict the output or state transition.",
          "Type the example yourself and run the narrowest possible verification.",
          "Compare actual evidence with your prediction. If they differ, stop and explain why before continuing.",
          "Replace the toy value with a TaskFlow-oriented case so Persistent Task CRUD is connected to the real course project.",
          "Complete the controlled failure lab, fix it, and rerun the same verification.",
          "Close the solution and rebuild the core behavior from memory."
        ],
        "commonMistakes": [
          {
            "mistake": "Copying a large finished block before understanding the first change.",
            "fix": "Type one small slice, run it, and explain the evidence before adding the next slice."
          },
          {
            "mistake": "Changing several files after one error.",
            "fix": "Find the first boundary where expected and actual evidence differ, then change only that cause."
          },
          {
            "mistake": "Memorizing syntax without understanding ownership.",
            "fix": "Explain why Persistent Task CRUD belongs in this boundary and what would go wrong if the same rule were duplicated elsewhere."
          },
          {
            "mistake": "Testing only the happy path.",
            "fix": "Deliberately violate one assumption and confirm the failure is understandable and controlled."
          }
        ],
        "recap": [
          "You should now be able to describe Persistent Task CRUD simply.",
          "You should know how it fits into the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit.",
          "You should have one working example, one deliberate failure, and one independent variation."
        ]
      }
    }
  ],
  "quiz": [
    {
      "question": "Which statement best explains “Prisma Schema Anatomy” in this track?",
      "options": [
        "Prisma Schema Anatomy is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "Generated Prisma Client is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "PrismaService in NestJS is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "First Task Model is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
      ],
      "answer": 0,
      "explanation": "Prisma Schema Anatomy: Prisma Schema Anatomy is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
    },
    {
      "question": "Which statement best explains “Generated Prisma Client” in this track?",
      "options": [
        "PrismaService in NestJS is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "First Task Model is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command.",
        "Generated Prisma Client is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
      ],
      "answer": 3,
      "explanation": "Generated Prisma Client: Generated Prisma Client is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
    },
    {
      "question": "Which statement best explains “PrismaService in NestJS” in this track?",
      "options": [
        "A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command.",
        "Inspect Generated SQL is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "PrismaService in NestJS is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "First Task Model is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
      ],
      "answer": 2,
      "explanation": "PrismaService in NestJS: PrismaService in NestJS is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
    },
    {
      "question": "Which statement best explains “First Task Model” in this track?",
      "options": [
        "Relations is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "First Task Model is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command.",
        "Inspect Generated SQL is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
      ],
      "answer": 1,
      "explanation": "First Task Model: First Task Model is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
    },
    {
      "question": "Which statement best explains “Development Migrations” in this track?",
      "options": [
        "A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command.",
        "Inspect Generated SQL is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "Relations is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work."
      ],
      "answer": 0,
      "explanation": "Development Migrations: A migration is a versioned database change. The important skill is understanding the SQL and rollout risk, not merely running an ORM command."
    },
    {
      "question": "Which statement best explains “Inspect Generated SQL” in this track?",
      "options": [
        "Relations is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work.",
        "Seed Data is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "Inspect Generated SQL is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
      ],
      "answer": 3,
      "explanation": "Inspect Generated SQL: Inspect Generated SQL is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
    },
    {
      "question": "Which statement best explains “Relations” in this track?",
      "options": [
        "Seed Data is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows.",
        "Relations is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work."
      ],
      "answer": 2,
      "explanation": "Relations: Relations is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
    },
    {
      "question": "Which statement best explains “Indexes in Prisma” in this track?",
      "options": [
        "A schema change is dangerous when old application code or existing rows cannot satisfy the new schema immediately. Safe rollouts often expand, backfill, verify, then contract.",
        "An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work.",
        "Seed Data is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows."
      ],
      "answer": 1,
      "explanation": "Indexes in Prisma: An index is an additional data structure PostgreSQL maintains to make selected lookup and ordering patterns faster, at the cost of storage and write work."
    },
    {
      "question": "Which statement best explains “Seed Data” in this track?",
      "options": [
        "Seed Data is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows.",
        "A schema change is dangerous when old application code or existing rows cannot satisfy the new schema immediately. Safe rollouts often expand, backfill, verify, then contract.",
        "Staged Backfills is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
      ],
      "answer": 0,
      "explanation": "Seed Data: Seed Data is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly."
    },
    {
      "question": "Which statement best explains “Prisma Transactions” in this track?",
      "options": [
        "A schema change is dangerous when old application code or existing rows cannot satisfy the new schema immediately. Safe rollouts often expand, backfill, verify, then contract.",
        "Staged Backfills is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "Production Migration Strategy is one piece of the Prisma layer that maps application code to PostgreSQL while keeping migrations explicit. The practical goal is to understand what it owns, what goes in, what comes out, and how you can prove it behaves correctly.",
        "A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows."
      ],
      "answer": 3,
      "explanation": "Prisma Transactions: A database transaction groups operations so they commit together or roll back together, protecting invariants when one business action touches multiple rows."
    }
  ]
}
